@model IList<ElsevierMaterialsMVC.Models.Shared.PropertyDescription>

<div>
    <table class="details" style="width:100%;">
        @*<thead>
                <tr>
                    <th>

                    </th>
                    <th>

                    </th>
                </tr>
            </thead>*@  
        @foreach (ElsevierMaterialsMVC.Models.Shared.PropertyDescription item in Model)
        {
            <tr>
                <td style="width:20%;">@Html.Raw(item.Name)</td>
                <td>@Html.Raw(item.Text)</td>
            </tr>
        }

    </table>
</div>